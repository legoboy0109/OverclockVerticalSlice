# Tech Debt Register

Advisory deviations and forward-looking items logged at story close. Each entry
names the story it was raised from. Track/triage via `/tech-debt`.

- **2026-07-26** (GS-001 GameState Core; AP-001/AP-002 AP Economy): raw `per_player[player]` indexing with no bounds check across the whole per-player read surface — `GameState.current_ap(player)`/`faction_of(player)` (GS-001) and `AP.current_ap`/`can_afford`/`spend`/`income`/`ap_income_breakdown` (AP-001/002). An out-of-range player index throws an unhandled runtime error rather than returning a sentinel. Undocumented "trusted internal API" contract. **Make ONE cross-cutting ruling** for the whole surface: either (a) document + regression-test the crash-on-bad-caller behavior as intentional, or (b) add bounds guards. ADR-0006/ADR-0001 don't mandate caller-side validation. Both `/code-review` passes (GS-001, AP-002) flagged this independently. — tracked from `production/epics/game-state-turn-manager/story-001-gamestate-core-model-read-clone.md`, `production/epics/ap-economy/story-002-ap-spend-contract.md`
- **2026-07-26** (GS-001 GameState Core): `entities_by_id` is a plain untyped `Dictionary`; `entity_at()`'s `entities_by_id.get(id)` returns `Variant` coerced to `-> EntityState`, safe only by write-discipline convention. ADR-0001 (Risks, line 269) marks typed `Dictionary[int, EntityState]` as RESOLVED/available (Godot 4.4+). Tighten to the typed dict when Story 002 starts writing to it from multiple call sites, to make the return statically sound. — tracked from `production/epics/game-state-turn-manager/story-001-gamestate-core-model-read-clone.md`
- **2026-07-26** (GS-001 GameState Core): `entity_at()` occupancy/entities desync path — if `grid.occupant_at` returns a valid id absent from `entities_by_id`, it degrades safely to `null` (no crash) but is untested. No mutator exists yet to produce the desync; add a regression test alongside Story 002's `apply_action`/`place`/`remove` mutators. — tracked from `production/epics/game-state-turn-manager/story-001-gamestate-core-model-read-clone.md`
- **2026-07-26** (AP-001 AP Income): AC-8's faction income-delta fold (`Δ_base`/`Δ_tier1`/`Δ_tier2`) + `BASE_INCOME_FLOOR` guard is DEFERRED — governing ADR-0006 Risks explicitly punts it to the Alpha faction-asymmetry prototype (ADR-0012). When the Faction Identity epic lands, it must add the delta fields to `FactionDef` + a `BASE_INCOME_FLOOR` constant and fold them into `AP.ap_income_breakdown()`, then add the fold/floor tests AP-001 could not. — tracked from `production/epics/ap-economy/story-001-ap-income-econconfig-balance.md`
- **2026-07-26** (AP-001 AP Income): AC-5's real "`has_economy_tech` must not default true / be ignored" regression is a Research-layer concern — `AP.income()` never reads the flag (gating lives in `Research.economy_tech_income_bonus()` per ADR-0006). The future Research epic's test suite owns this regression; do NOT assume AC-5 is fully covered by AP-001 alone. — tracked from `production/epics/ap-economy/story-001-ap-income-econconfig-balance.md`
- **2026-07-26** (Shared test fixtures): the `BaseProduction`/`Research` stubs use process-global `static var` state shared across all consuming suites (AP-001/AP-003/GS-003). Isolation holds only because each suite calls `.reset()` in `before_test`. Add a convention note to `tests/README.md` mandating `reset()` in every consumer's `before_test`, and revisit if GdUnit4 ever parallelizes test-file execution within one process. — tracked from `tests/helpers/stubs/`
- **2026-07-26** (AP-002 → AP-003 handoff): AP-002's `0 ≤ current_ap ≤ income_this_turn` invariant test only proves the **lower** bound (`spend` only decreases `current_ap`, so it can't violate the upper bound by construction). AP-003's `reset_turn` QA must add the real **upper-bound** test: assert `reset_turn` re-establishes `current_ap == income_this_turn` even when `current_ap` starts *above* the new income (a stale-higher value from a hypothetical no-discard bug), proving reset re-caps rather than merely not-exceeding by accident. — tracked from `production/epics/ap-economy/story-002-ap-spend-contract.md`
- **2026-07-26** (GS-002 → GS-003 faction-lock seam): `GameState.is_faction_locked()` returns `true` **unconditionally** — `MatchStatus` has no `SETUP` value and no faction-assignment verb exists yet, so the `OK` (pre-lock) branch of `check_faction_reassignment()` is unreachable and untested. AC7's test is a guard-seam placeholder, not a real Setup→PlayerTurn transition test. GS-003's `start_match()`/Setup FSM (ADR-0008/0012) MUST replace the unconditional `true` with a real phase check and add a test exercising the `OK` branch before the lock engages. — tracked from `production/epics/game-state-turn-manager/story-002-apply-action-pipeline.md`
- **2026-07-26** (GS-002 thin tests → Story 003+): AC3 (determinism) and AC4 (idempotency-by-revalidation) tests are thin because `_apply_end_turn` is a no-op stub this story — AC3 applies a single action touching no order-sensitive structures, and AC4 effectively re-tests the active-player gate (no side effect to double-apply). Re-strengthen both once a real state-mutating verb lands (Story 003's real end-of-turn sequence, or the first Movement/Combat verb): AC3 should sequence multiple actions and compare full state incl. `entities()` ordering; AC4 should prove no compounding side effects on re-submit. — tracked from `production/epics/game-state-turn-manager/story-002-apply-action-pipeline.md`
- **2026-07-26** (GS-002 illegal-target coverage): QA Test Cases bullet 2's "illegal target" half has no test (unlike the affordability half, which got a synthetic stub). No concrete targeted verb exists yet, so it is deferred — the first verb with a target concept (Movement/ADR-0009 or Combat/ADR-0010) must add the illegal-target rejection + zero-state-change test. — tracked from `production/epics/game-state-turn-manager/story-002-apply-action-pipeline.md`
- **2026-07-26** (GS-003 HQ-type / StructureState seam): `start_match` constructs HQs as plain `EntityState` (not `StructureState`) so `src/` never depends on `tests/helpers/stubs/` — correct dependency direction, but HQs consequently skip `start_turn`'s step-2 `is UnitState`/`is StructureState` flag-reset dispatch. Inert today (base `EntityState` has no per-turn flag). When ADR-0007's real `UnitState`/`StructureState` entity schema lands, revisit: HQs must become the real `StructureState` and start participating in step-2 dispatch if any per-turn structure flag applies to them. Covered-as-skipped by `test_start_turn_step2_safely_skips_plain_entity_state_neither_unit_nor_structure`. — tracked from `production/epics/game-state-turn-manager/story-003-turn-fsm-start-of-turn-sequence.md`
- **2026-07-26** (GS-003 completion-event ownership): `StructureCompletedEvent`/`TechCompletedEvent` were created in `src/core/event/` by GS-003 (so the step-3 stubs had a concrete type to emit + the "events flow through action_applied" test had a type), but arguably belong to the Base & Production / Research epics respectively. When those epics land, reconcile: their real `advance_build_timers`/`advance_research_timers` may extend these events with fields (`structure_id`/`structure_type`, `player`/`tech` — ADR-0007), but the files can stay in `src/core/event/` regardless of owning system. — tracked from `production/epics/game-state-turn-manager/story-003-turn-fsm-start-of-turn-sequence.md`
- **2026-07-26** (GS-003 step-3 commutativity has no failing test): the manifest requires `start_turn` step 3's build-vs-research timer-advance calls "stay commutative," but step 3 hardcodes the call order with no seam to permute, so no test can fail on an accidental reorder — the invariant is enforced only by ADR-0008 + code review. `test_income_total_same_regardless_of_which_system_supplies_the_completion` (renamed from a misleading "commutative" name) only proves `AP.income` treats both bonus sources additively. To make it test-enforced, refactor step 3 to accept an orderable `Array[Callable]` of timer-advancers and add a permutation test. — tracked from `production/epics/game-state-turn-manager/story-003-turn-fsm-start-of-turn-sequence.md`
- **2026-07-26** (GS-004 StructureDestroyedEvent ownership): `run_win_check` consumes `StructureDestroyedEvent{is_hq, owner}`, created in `src/core/event/` by GS-004 so the win-check had a concrete type to scan before Combat exists. Its real owner is Combat/ADR-0010 — whose `destroy_entity()` hook is the authoritative producer and whose ADR-0010 interface sketch adds `entity_id` (constructor-injected). When the Combat epic lands, reconcile: extend this event with `entity_id`, wire the real producer, and confirm `run_win_check`'s `is_hq` scan still matches. File can stay in `src/core/event/`. — tracked from `production/epics/game-state-turn-manager/story-004-win-check-terminal-conditions.md`
- **2026-07-26** (GS-004 tiebreak metric is UNIT_COUNT-only): `GameState.TiebreakMetric` enum has only `UNIT_COUNT` (count of entities per player) — the story's other listed metrics (total HQ hp, tiles controlled) need ADR-0007's entity-stat schema (hp) and grid-ownership queries, neither built yet. `_compute_tiebreak_metric`'s `match` has an intentional documented fallback-to-tie for an unrecognized metric. When ADR-0007 lands, add the hp/tiles metrics + a `tiebreak_metric` config decision (which is the VS default) + tests. — tracked from `production/epics/game-state-turn-manager/story-004-win-check-terminal-conditions.md`
- **2026-07-26** (tooling — stale global-class-cache gotcha, cross-cutting): adding a new `class_name` `.gd` file (e.g. GS-004's `StructureDestroyedEvent`/`GameOverEvent`) can trigger "Could not find type X" parse errors on a headless `--script` run until `./redot --headless --import` rebuilds `.godot/global_script_class_cache.cfg`. Purely engine tooling, not a code defect, but it bit GS-004 mid-implementation. Consider a `tests/README` note or a pre-test hook that runs `--import` when new `class_name` files are detected. — tracked from `production/epics/game-state-turn-manager/story-004-win-check-terminal-conditions.md`
- **2026-07-26** (Unit-003 `duplicate`→`clone` GDD/registry reconciliation): the unit deep-copy op is named `duplicate(unit)` in `design/gdd/unit-system.md` Rule 2a and in the TR-unit-003/004 registry text, but that name is **unusable in GDScript** — a `class_name` script is itself a `Resource`, so the built-in `Resource.duplicate(bool)` shadows a static `duplicate` when called as `Unit.duplicate(x)` (runtime: "Cannot convert argument 1 from Object to bool"). Implemented as `Unit.clone(unit)` (matches `GameState.clone()`; user-approved 2026-07-26). **Reconcile the docs to match the code**: update `unit-system.md` Rule 2a and the TR-unit-003/004 `requirement` text to say `clone` (via `/propagate-design-change` or a direct edit), so the GDD/registry don't drift from the shipped API that the AI epic/ADR-0011 will call. — tracked from `production/epics/unit-system/story-003-pure-ops-hp-delta.md`
