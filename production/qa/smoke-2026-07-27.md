# Smoke Check Report

**Date**: 2026-07-27
**Sprint**: Sprint 2 (planning / de-risking sprint — QA plan deferred to Sprint 3 VS)
**Engine**: Redot 26.2 (Godot 4.x-compatible)
**CI**: Configured (`.github/workflows/tests.yml`)
**QA Plan**: `production/qa/qa-plan-sprint-1-2026-07-26.md` (sprint-1; sprint-2 defers QA to Sprint 3 per its plan)
**Argument**: sprint

---

## Automated Tests

**Status**: PASS — **504 / 504 test cases** across 43 suites | 0 errors | 0 failures | 0 flaky | 0 skipped | 0 orphans | exit 0.
Command: `./redot --headless --script tests/gdunit4_runner.gd` (ran in ~3.5s).

No failing tests. The trailing `ObjectDB`/`PagedAllocator` "leaked at exit" lines are Redot headless-shutdown noise (present on every run of this project), unrelated to test outcomes.

---

## Test Coverage

| System | Test files | Coverage Status |
|--------|-----------|-----------------|
| Base & Production (stories 001–010) | 8 unit (`tests/unit/base-production/*`) + 1 integration (`tests/integration/base-production/integration_apply_action_end_to_end_test.gd`) | COVERED |
| Foundation — Grid | `grid_build_authored`, `grid_mutation_api`, `grid_procedural_generation`, `grid_query_api` | COVERED |
| Foundation — GameState / Turn | `game_state_core`, `turn_sequencing`, `apply_action_pipeline`, `win_check_terminal` | COVERED |
| Foundation — AP Economy | `ap_income`, `ap_spend`, `ap_reset_discard` | COVERED |
| Unit system | `unit_state`, `unit_ops`, `unit_typedef`, `unit_cost_fields`, `effective_attack`, `effective_defense`, + `unit-system/*` (lifecycle, serialization, read-surface, faction read-sites) | COVERED |
| Movement | `movement/move_action`, `movement/reachable_search` + `integration/movement/movement_determinism` | COVERED |
| Combat | `combat/*` (damage_formula, attack_pipeline_ap_cost, counterattack, direct/area targeting, destroy_entity, determinism) + `integration/combat/combat_apply_action_integration` | COVERED |

**Summary**: All implemented Logic/Integration stories have matching test files. **0 MISSING, 0 UNKNOWN.**

---

## Manual Smoke Checks

No interactive/playable build exists yet — the main menu, rendered isometric board, and interactive play are the **Sprint 3 Vertical Slice**. The critical-path items in `tests/smoke/critical-paths.md` that require a running game (1–6, 10–11) are therefore N/A this session; the underlying simulation logic is covered by the green automated suite.

- [-] Game launches to main menu without crash — **N/A** (no runnable build; Sprint 3 VS)
- [-] New match starts from main menu — **N/A** (no UI/menu yet; Sprint 3 VS)
- [-] Main menu responds to inputs — **N/A** (no UI yet)
- [-] Turn loop (start turn / AP income snapshot / End Turn) — **N/A interactive**; simulation covered by `turn_sequencing_test`, `apply_action_pipeline_test`, and Base-Production integration gate (Rule-6 ordering)
- [-] Movement (reachable set, atomic occupancy) — **N/A interactive**; covered by `movement/move_action_test`, `movement/reachable_search_test`, `integration/movement/movement_determinism_test`
- [-] Combat (attack resolves, HP updates, HQ→0 triggers synchronous GameOver) — **N/A interactive**; covered by `combat/*` unit tests + `combat/combat_apply_action_integration_test` + Base-Production `structure_destruction_hq_win_hook` / integration HQ-win test
- [x] AI `clone()` of GameState produces an independent state (no aliasing) — **PASS** (automated: `base-production/determinism_clone_isolation_test`, `combat/determinism_test`)
- [-] Save / load — **N/A** (deferred by architecture — no save system yet)
- [-] Performance / frame rate / memory — **N/A** (no rendered build to profile)

---

## Missing Test Evidence

None — all Logic and Integration stories have test coverage.

---

## Verdict: **PASS**

The automated suite is fully green (504/504, exit 0) with complete test coverage of every implemented system; there is no missing test evidence and no known regression. Interactive/manual smoke is **N/A pre-Vertical-Slice** and deferred to Sprint 3 per the Sprint 2 plan.

This is a **code-level PASS**. The interactive critical-path smoke (launch → play one turn → resolve → win-check) runs for the first time once the Vertical Slice build exists (Sprint 3). Re-run `/smoke-check sprint` against that build before its QA hand-off.

### Session context

This smoke check follows completion of the **Base & Production epic** (stories 001–010; 003 Superseded) this session — the final two stories (009 Production HUD read-surface, 010 apply_action end-to-end integration gate) were implemented, reviewed (APPROVED), and closed. The epic's BLOCKING integration gate confirmed all B&P verbs compose correctly through the real `apply_action`/`start_turn`/`destroy_entity` stack with no `src/` change required.
