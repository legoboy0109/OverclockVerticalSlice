## Smoke Check Report
**Date**: 2026-07-26
**Sprint**: Sprint 1 (Foundation — Game State & Turn Manager + AP Economy)
**Engine**: Redot 26.2 (Godot 4.6-compatible)
**QA Plan**: `production/qa/qa-plan-sprint-1-2026-07-26.md`
**Argument**: sprint

---

### Automated Tests

**Status**: PASS — 177 tests, 177 passing, 0 failures, 0 errors, 0 flaky, 0 skipped. 12 suites executed, exit code 0.

Command: `./redot --headless --script tests/gdunit4_runner.gd`

CI: configured at `.github/workflows/tests.yml` (runs the same runner on push/PR).

---

### Test Coverage

| Story | Type | Test File | Coverage Status |
|-------|------|-----------|----------------|
| GS-001 GameState Core | Logic | `tests/unit/game_state_core_test.gd` | COVERED |
| GS-002 apply_action Pipeline | Logic | `tests/unit/apply_action_pipeline_test.gd` | COVERED |
| GS-003 Turn FSM | Logic | `tests/unit/turn_sequencing_test.gd` | COVERED |
| GS-004 Win-Check / GameOver | Logic | `tests/unit/win_check_terminal_test.gd` | COVERED |
| AP-001 Income Formula | Logic | `tests/unit/ap_income_test.gd` | COVERED |
| AP-002 Spend Contract | Logic | `tests/unit/ap_spend_test.gd` | COVERED |
| AP-003 reset_turn/discard | Logic | `tests/unit/ap_reset_discard_test.gd` | COVERED |

**Summary**: 7 covered, 0 manual, 0 missing, 0 expected.

Additional passing suites from the prior Grid & Terrain epic (not in this sprint's scope but part of the 177): `grid_build_authored_test.gd`, `grid_mutation_api_test.gd`, `grid_procedural_generation_test.gd`, `grid_query_api_test.gd`.

---

### Manual Smoke Checks (`tests/smoke/critical-paths.md`)

**Context**: Sprint 1 delivered the headless simulation core. There is no playable build, main menu, or rendering yet (render ADRs 0013–0016 are not implemented). Playable-build critical paths are therefore N/A at this stage, not failures — the sprint's deliverable is verified by the 177-test headless suite.

- [-] #1–3 Game launches to menu / new match from menu / menu input — **N/A** (no playable build yet)
- [x] #4 Turn loop — start-of-turn, AP resets to income snapshot, End Turn passes control — **PASS** (GS-003 implemented + test-covered by `turn_sequencing_test.gd`)
- [-] #5 Movement within reachable set, atomic occupancy — **N/A** (ADR-0009, future epic)
- [~] #6 Combat / HQ-at-0-HP → synchronous GameOver — **PARTIAL**: the win-check GameOver path is implemented + tested (GS-004, `win_check_terminal_test.gd`, via forward-declared `StructureDestroyedEvent`); the combat attack/HP resolution that *produces* the destruction is a future epic (ADR-0010)
- [-] #7–8 Save / load — **N/A** (deferred per architecture)
- [x] #9 `GameState.clone()` produces an independent state (no aliasing) — **PASS** (test-covered; developer confirmed no aliasing observed)
- [-] #10–11 Performance / memory over time — **N/A** (headless; no runtime loop yet, 60 FPS target applies once rendering lands)
- [x] No suite crash, no unexpected regression, CI green — **PASS** (developer confirmed nothing failed)

---

### Missing Test Evidence

All Logic and Integration stories have test coverage. None missing.

---

### Verdict: **PASS**

Automated tests pass cleanly (177/177, exit 0). All applicable smoke checks pass; playable-build critical paths are N/A at the Foundation stage rather than failures. No missing test evidence. The build (headless simulation core) is ready for manual QA hand-off.

**QA hand-off**: share `production/qa/qa-plan-sprint-1-2026-07-26.md` with the qa-tester agent — though note every Sprint-1 story lists "Manual Verification Required: None," so the QA cycle for this sprint is primarily test-coverage/evidence review rather than manual playtest.
