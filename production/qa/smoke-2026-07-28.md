## Smoke Check Report
**Date**: 2026-07-28
**Sprint**: Sprint 3 — OVERCLOCK Vertical Slice Build
**Engine**: Redot 26.2 (Godot 4.6-compatible)
**QA Plan**: `production/qa/qa-plan-sprint-3-2026-07-27.md`
**Argument**: sprint

---

### Automated Tests

**Status**: **PASS** — 826 tests, 826 passing, 0 failures, 0 errors, 0 orphans, 74 suites.
Run headless via `./redot --headless --script tests/gdunit4_runner.gd` (exit 0).

> Grew from the 811 in-slice baseline to 826 as post-hand-off windowed defects were
> fixed with regression tests (AI freeze/oscillation, non-Scout movement, produce
> readiness, action feedback, camera framing, overlay glyphs, demo-occupant removal).

---

### Test Coverage

All 24 Logic/Integration stories have their test file present at the QA-plan path
(verified on disk):

**Summary**: 24 covered, 2 manual (BR-002, HUD-005 Visual/Feel-primary — evidence
docs present), 0 missing, 0 expected. No MISSING test evidence.

---

### Manual Smoke Checks

Executed by the developer on a windowed build (pulled from `main`, run in the
Redot 26.2 editor). The build is script-only with placeholder rendering; a windowed
run was required because the session host is headless/SSH.

- [x] **Game boots** — the scene loads straight into a live match; board + HUD render, no crash — **PASS**
- [x] **New match starts** — 12×10 map, both HQs placed, live GameState — **PASS**
- [x] **Input live** — keyboard cursor moves; Enter selects an own unit (detail panel + range highlight) — **PASS**
- [x] **Full loop — Move** — select a unit → move to a reachable (blue) tile; AP deducts; unit relocates. All unit types (Scout/Trooper/Heavy/Sniper) — **PASS**
- [x] **Full loop — Attack** — select a unit → attack an in-range enemy; damage applies; AP deducts — **PASS**
- [x] **Full loop — Produce** — produce the selected unit type from a completed producer (P/V); new unit appears; AP deducts — **PASS**
- [x] **Full loop — Build** — build the selected structure type (B/C); structure placed; AP deducts — **PASS**
- [x] **Turn handoff** — Tab ends the turn → AI takes a full, credible turn (produce/advance/attack) → control returns; round advances; no freeze — **PASS**
- [x] **HUD stays live** — AP counter, action log, build/produce readouts, and the status/legend overlay reflect real state throughout — **PASS**
- [-] **Save / load** — N/A (save system deferred per architecture; out of VS scope)
- [-] **Performance** — not formally profiled this session; no visible hitching reported at VS scale

---

### Missing Test Evidence

All Logic and Integration stories have test coverage. Advisory Visual/Feel and UI
sign-offs (BR-002/003/005, CAI-006, HUD-004/005/007) remain deferred to a windowed
sign-off pass — evidence stubs exist under `production/qa/evidence/`. These are
ADVISORY (per the story-type gate table) and do not block QA hand-off.

---

### Verdict: **PASS**

- Automated suite passes (826/826, 0 failures).
- All Batch 1 (core stability) and Batch 2 (sprint mechanic + regression) manual
  checks pass, confirmed on a windowed build.
- No MISSING Logic/Integration test evidence.

The build is ready for manual QA hand-off. The full vertical-slice loop
(select → move/attack → produce → build → end turn → AI reply → win-check) is
demonstrable end-to-end.

**Note carried to the phase gate:** this smoke PASS validates the *build*. The
sprint's PROCEED/PIVOT/KILL verdict additionally depends on the iso-legibility and
swing-back **playtests** (S3-09/10/11), which are not yet run and are the sanctioned
Sprint-4 rollover (see `production/retrospectives/retro-sprint-3-2026-07-28.md`).
