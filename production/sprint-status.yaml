# Auto-generated by /sprint-plan. Updated by /story-done and /dev-story.
# DO NOT edit manually — use /story-done to update story status.
#
# Status value mapping (yaml <-> story file Status field):
#   backlog / ready-for-dev / in-progress / review / done / blocked
#
# ============================================================================
# SPRINT 6 — PROVE THE PIVOT FIX. Plan: production/sprints/sprint-6.md
# ============================================================================
# The vertical slice returned PIVOT 2026-08-24 (production/vertical-slice/REPORT.md).
# Root cause, measured: the economy is unbounded, so building always outscores
# fighting, so nobody marches on the objective, so no game is ever won. Credits
# peaked at 5,724 still climbing; ZERO HQ damage across 4,182 turn-rows, and zero
# again across 1,260 more after a siege drive was added and PROVEN to fire.
#
# ✅ THE GATE PASSED 2026-08-24, batch 5 (2626f6d, merged ae120bc). Baseline ->
#   result: resolve-on-play 0/20 -> 18/21 (mean 25 turns); HQ damage zero across
#   4,182 rows -> lowest HQ 1/40 with 608 damage-carrying rows; seat split 95-100%
#   one seat -> P0 9 / P1 9; handicap conversion 0/5 -> 18/18; peak Credits 5,724
#   and CLIMBING -> 9,550 and FLAT.
#   ★★ THE METHOD LESSON, and it is the sprint's most valuable output: four levers
#     were tried; THREE found and fixed genuine defects and moved resolution NOT AT
#     ALL (bounding the economy, implementing CREDIT_TO_AP_RATE, valuing production
#     capacity). The two that worked were about INCENTIVES, not correctness --
#     production cooldowns, and decisively hq_siege_value 12 -> 60. A correct system
#     that rewards the wrong thing plays exactly like a broken one.
#   ⚠ Read 18/18 handicap conversion SCEPTICALLY: the gate asked for "more often
#     than not" and got TOTAL correlation, which suggests no comeback mechanism.
#   ⚠ The 3 distance games are the +0 mirror, and that cell is n=1 NOT n=3 --
#     `variant` perturbs bonus-unit placement and at +0 there are no bonus units,
#     so all three runs are byte-identical. Fix the harness before concluding
#     anything from that cell.
#
# ★★ TWO CONSTANTS MUST BE CORRECTED BEFORE ANY MEASUREMENT IS TRUSTED
#   (cross-review 2026-08-24, design/gdd/gdd-cross-review-2026-08-24.md):
#   1. CREDIT_TO_AP_RATE 1.0 -> 0.01. The x100 Credit rescale broke its derived
#      1:1 anchor. At 1.0 the AI values building/killing ~100x a march -- the
#      EXACT PIVOT failure mechanism amplified. A batch at 1.0 measures an agent
#      that cannot manoeuvre and would make the fix look like it failed.
#   2. UPKEEP_DIVISOR needs UPKEEP_GRANULARITY. ceil(cost/3) rounded hard on
#      small numbers; at x100 it drifts low (67/134/167/234 instead of
#      100/200/200/300), raising the sustainable army ~9 -> ~12 against a cap of
#      10 and silently breaking the cap/upkeep relationship.
#   ★ Neither was findable by grepping stale names. Both are conversion/rounding
#     constants that a proportional rescale does not carry safely.
#
# ★ NOT IN THIS SPRINT, deliberately: any faction content. Under faction-identity
#   CR-11 every faction needs piloted vehicles and the infantry cap, so nothing
#   ships before unit classes + pilots (wave 2). An infantry-only faction is a
#   fragment, not a test. Route new design to production/post-gate-backlog.md --
#   Sprint 4 lost its entire window to exactly that drift.
#
# SPRINT 5 CLOSED 6/10: s5-01/02/05/06/08 done, s5-07 review (captures done,
#   human sign-off owed). s5-03 (iso-legibility) and s5-04 (swing-back) remain
#   HUMAN-GATED and have now rolled FOUR sprints. ★★ s5-04's deferral condition is
#   NOW MET (2026-08-25): the s6-06 gate passed, so the AI can actually lose and a
#   session finally measures a real game. It is unblocked and is the highest-value
#   item on the board -- and it still needs the user at a display.
#   s5-09/s5-10 descoped (nice-to-haves, recorded trade).

sprint: 6
goal: "Make matches resolve on play: implement the economy rework (upkeep, research-driven income, per-structure maximums) and prove it with the AI-vs-AI batch that diagnosed the failure."
start: "2026-08-25"
end: "2026-09-08"
generated: "2026-08-24"
updated: "2026-08-25"
# Committed 7.5d (must 5.5 + should 2.0) against ~8 available. Nice-to-haves ride the buffer.
# Design corpus backing this sprint: unit-upkeep.md (#15), population-cap.md (#16),
# ap-economy.md + research-tech.md + base-production.md (all In Revision 2026-08-24).

stories:
  # --- Must Have: the critical path ---
  - id: "s6-01"
    name: "Economy re-base — delete outpost income, add three research tiers"
    file: "design/gdd/ap-economy.md"
    priority: must-have
    status: done
    owner: "gameplay-programmer"
    estimate_days: 1.0
    blocker: ""
    completed: "2026-08-24"
    # ✅ DONE. Suite 979/979 (was 984 pre-change; net -5 because the outpost-curve
    # tests were replaced by a smaller, sharper set). 0 failures, 0 orphans; slice
    # boots clean (exit 0). Branch feat/s6-01-economy-rebase.
    # credit_income = BASE_INCOME(1000) + 500 per completed economy tier. Tiers cost
    # 1000/2000/3500, strictly sequential. Hard ceiling 2500 -- FINITE BY CONSTRUCTION,
    # which is the half of the PIVOT fix that bounds the RATE. Remove OUTPOST_BONUS_TIER1/2,
    # TIER_THRESHOLD, ECONOMY_TECH_INCOME_BONUS, ECONOMY_TECH_TIER_THRESHOLD entirely.

  - id: "s6-02"
    name: "Unit upkeep — net_credit_income, deficit lock, disband"
    file: "design/gdd/unit-upkeep.md"
    priority: must-have
    status: done
    owner: "gameplay-programmer"
    estimate_days: 1.5
    blocker: ""
    completed: "2026-08-24"
    # ✅ DONE. Suite 1011/1011, 0 errors/failures/orphans; slice boots clean (exit 0).
    # 31 new tests across upkeep_test.gd + disband_and_deficit_lock_test.gd.
    # ⚠ OVERRAN ITS ESTIMATE, and almost none of it was the feature -- see the
    #   "Recorded during the sprint" section in sprint-6.md. The x100 Credit rescale
    #   was INCOMPLETE from s6-01 (data files were never rescaled: a Scout cost 2
    #   against an income of 1000), and completing it broke ~20 hardcoded cost
    #   literals plus several underfunded test fixtures.
    # ★ FOUND A REAL BUG IN MY OWN CODE via the suite: total_upkeep crashed on an
    #   entity with a null `type`. start_turn step 2 is explicitly required to skip a
    #   bare EntityState safely and the economy step iterates the SAME list, so it must
    #   tolerate them too. Guarded + regression test added.
    # ★ Several "failures" were upkeep WORKING: tests asserting gross income where net
    #   is now correct (1000 -> 900 = income minus a Scout's upkeep). Rewritten to
    #   derive from Upkeep; asserting gross there would now be asserting a bug.
    # The half of the PIVOT fix that bounds the STOCK. Neither half works alone: a hard
    # income ceiling with no drain still climbs linearly, which is exactly what the
    # simulation measured at 5,724.
    # ★ Derived upkeep MUST use UPKEEP_GRANULARITY (100) and yield 100/200/200/300 for
    #   Scout/Trooper/Sniper/Heavy. Without it the whole cap/upkeep relationship breaks.
    # Deficit rule (UR-6): bank floors at 0, produce/build/research lock, NO attrition --
    # a death spiral is the opposite of the comeback pressure this system exists to create.
    # Disband (UR-7) is the escape valve that makes UR-6 survivable: 1 AP, 50% refund.

  - id: "s6-03"
    name: "Structure rework — Barracks/Factory rename, per-structure maximums"
    file: "design/gdd/base-production.md"
    priority: must-have
    status: done
    owner: "gameplay-programmer"
    estimate_days: 1.0
    blocker: ""
    completed: "2026-08-24"
    # ✅ DONE. Suite 1024/1024, 0 errors/failures/orphans; slice boots clean (exit 0).
    # 13 new tests (structure_maximums_test.gd). ★ ZERO collateral churn, unlike s6-01/s6-02:
    #   max_count defaults to 0 = unlimited, so adding the field changed nothing until a
    #   type opted in; and RENAMING rather than deleting meant 27 referencing files updated
    #   mechanically instead of each needing a replacement chosen.
    # ★ Economy Outpost -> FACTORY (reuses its art, which is what the user's art decision
    #   already implied), Production Outpost -> BARRACKS. Art files renamed too.
    # ⚠ CARRIED, not done here: the Airfield (no air units to produce until unit-classes,
    #   wave 2) and Barracks cap_bonus (s6-04 wires it with the cap system).
    # ⚠ completed_outpost_count() is now DEAD product code -- income no longer calls it and
    #   nothing in src/ does; superseded by the generic structure_count(). Left in place
    #   only because 6 tests still name it as their subject. Removal is a tidy-up candidate,
    #   deliberately NOT bundled here so the gate-critical change stays reviewable alone.
    # ★ The per-structure MAXIMUMS are the load-bearing part, not the rename. The PIVOT
    #   was caused by the AI always having another thing worth building. With every
    #   structure capped, an Alliance full build-out is 10 structures / 7,300 Credits and
    #   then there is NOTHING LEFT TO BUILD -- at which point AP has nowhere to go but
    #   movement. Maxing everything is deliberately unaffordable (1,400 upkeep vs 2,500
    #   income leaves too little for an army), so infrastructure vs army is a real choice.
    # ⚠ Economy Outpost is referenced in .tres data, tests and the AI. Grep before deleting.

  - id: "s6-04"
    name: "Population cap — base cap, cap_per_barracks, max_barracks, produce gate"
    file: "design/gdd/population-cap.md"
    priority: must-have
    status: done
    owner: "gameplay-programmer"
    estimate_days: 1.0
    blocker: ""
    completed: "2026-08-24"
    # ✅ DONE. Suite 1039/1039, 0 errors/failures/orphans; slice boots clean (exit 0).
    # 15 new tests (population_cap_test.gd). Zero collateral churn -- the gate is at the
    # PRODUCE call site only, and existing fixtures place units directly rather than
    # producing them, so nothing existing tripped it.
    # ★ PC-3 ("units under production count") needed NO machinery: production is INSTANT
    #   (apply_produce creates and places the UnitState in one call), so there is no queue
    #   for a unit to sit in. Documented in Population.current_population as the place
    #   where it becomes real work if production ever goes multi-turn.
    # ★★ TWO FINDINGS while writing the cap/upkeep cross-check test, both recorded in
    #   population-cap.md:
    #   1. The "cap binds first" relationship is BUILD-DEPENDENT, not universal. A
    #      Barracks-only player maximises cap and minimises upkeep, so they sustain MORE
    #      than their ceiling (cap-bound, upkeep never bites). A diversified player is
    #      upkeep-bound. Both legitimate; the doc asserted it as universal.
    #   2. The doc's own worked example compared the FULL cap (3 Barracks) against a
    #      TWO-Barracks upkeep burden -- two different builds. Computed consistently the
    #      claim does hold (cap 10 vs ~9 sustainable). Test now computes both sides from
    #      ONE build, which is the only way the claim means anything.
    # ⚠ The Factory still carries the renamed Economy Outpost's stats (400/100/1 rather
    #   than the design's 1000/200/3) -- s6-03 renamed rather than re-statted deliberately.
    #   Re-statting shifts these numbers and is owed before they are trusted.
    # Cap = how many you may field; upkeep = how long you can afford them. Tuned as a pair:
    # ceiling 10 at full build-out, sustainable ~8-9 -- you can always field a little more
    # than you can comfortably keep. Queued units count (else the gate does nothing).
    # A Barracks dying LOWERS the cap without destroying units -- the owner is
    # production-locked until attrition resolves it.

  - id: "s6-05"
    name: "★★ AI re-anchor — CREDIT_TO_AP_RATE 0.01, economy_value on research tiers, lifetime cost"
    file: "design/gdd/ai-opponent.md"
    priority: must-have
    status: done
    owner: "ai-programmer"
    estimate_days: 1.5
    blocker: ""
    completed: "2026-08-24"
    # ✅ DONE. Suite 1039/1039, 0 errors/failures/orphans; slice boots clean (exit 0).
    # ★★ CREDIT_TO_AP_RATE WAS NEVER IMPLEMENTED -- it existed only in doc comments, and the
    #   code divided value by the RAW CREDIT cost (equivalent to rate 1.0). Implemented for
    #   real: credits_to_ap() + ap_equivalent_cost(), applied at every Credit-denominated
    #   value AND every economic denominator.
    # ★★ MY CROSS-REVIEW GOT B-5's DIRECTION PARTLY WRONG, corrected in the report:
    #   ATTACK inflated ~100x (Credit value / AP cost). PRODUCE cancelled (both sides x100)
    #   and survived by luck. BUILD is 0 (economy_value returns 0 since S6-01). MOVE
    #   unchanged. Only combat was distorted -- building never was.
    # ★★ AND A HAZARD THE REVIEW MISSED: hq_siege_value (12) is an AP-equivalent WEIGHT that
    #   did not rescale, while unit values became x100 Credit costs -- a Trooper kill would
    #   have scored ~30x an HQ strike, i.e. the exact "armies trade forever, nobody sieges"
    #   behaviour, amplified. Fixed by converting ONLY the Credit branches of the
    #   opponent-paid lookup (_opponent_paid_ap_equivalent) and leaving the HQ weight alone.
    # ★ Verified from shipped constants: economy tier 1.60 < LETHAL_FLOOR_BONUS 3.5 ✓;
    #   kill 3.00; HQ chip 0.75; produce 0.26; march 0.16 > PASS_THRESHOLD 0.15 ✓.
    #   All in ONE order of magnitude, which is the whole point of the rate.
    # ★ LIFETIME COST (UOQ-4): produce is now divided by purchase + upkeep x horizon, not
    #   the sticker price. Changed a documented AC from 1.10 to ~0.26 -- a REAL behaviour
    #   change, recorded in the test with its rationale: producing now correctly loses to a
    #   good attack, which it previously did not, while still clearing the pass bar.
    # ★ AI now mirrors validate_build/validate_produce's new gates (deficit lock, population
    #   cap, structure maximum) so the driver never generates a candidate that will be
    #   rejected on commit -- a reject loop stalls its turn.
    # ⚠ PREDICTION TO CHECK IN S6-06: a kill (3.00) still outscores an HQ chip (0.75) 4:1,
    #   so the AI sieges only when no unit is available to kill. That may be correct play,
    #   or it may be the mid-map trading pattern surviving. THE BATCH DECIDES.
    # ⚠ NOT in scope: OQ-15 (the AI cannot play six different armies on one weight set).
    # ★★ THE HIGHEST-RISK STORY IN THE SPRINT. It touches the most carefully-reasoned
    #   document in the corpus and its invariants must be RECOMPUTED, not assumed.
    # 1. CREDIT_TO_AP_RATE 1.0 -> 0.01 (cross-review B-5). Non-negotiable; see header.
    # 2. economy_value re-pointed off the deleted Economy Outpost onto research tiers.
    #    Projection machinery (ECONOMY_HORIZON 6, ECONOMY_DECAY 0.85) survives unchanged.
    #    An economy tier scores ~18.1 AP-equivalent; action_score ~1.65.
    # 3. Re-verify the lethal-floor invariant (1.65 < LETHAL_FLOOR_BONUS 3.5 -- headroom
    #    actually WIDENED from the old 1.77) and the Defense-Tech pass margin (~0.1675 vs
    #    PASS_THRESHOLD 0.15, unchanged since both terms scale together).
    # 4. AI must respect the population cap and the deficit lock, and value a unit at
    #    LIFETIME cost (purchase + upkeep x horizon), not purchase price -- otherwise it
    #    over-builds into deficit exactly as it over-builds today (UOQ-4).
    # ★ Note: with a finite three-tier tree the AI simply RUNS OUT of economy actions, so
    #   MAX_ECONOMY_INVESTMENTS_PER_TURN is largely moot -- the PIVOT fix working at the
    #   scoring layer as well as the rules layer.
    # ⚠ OQ-15 (AI cannot play six different armies on one weight set) is NOT in scope here.

  - id: "s6-06"
    name: "⛔ THE GATE — AI-vs-AI regression batch"
    file: "production/playtests/swing-back-simulation-appendix-2026-08-21.md"
    priority: must-have
    status: done
    owner: "qa-lead + ai-programmer"
    estimate_days: 0.5
    blocker: "s6-05"
    completed: "2026-08-24"
    # ✅ GATE PASSED, batch 5 (2626f6d, merged ae120bc). Resolve-on-play 18/21, mean 25
    # turns; lowest HQ seen 1/40 with 608 damage-carrying rows; kills split P0 9 / P1 9;
    # handicap converts 18/18; peak banked Credits 9,550 and FLAT (target <15,000).
    # ★★ FOUR levers were tried. THREE of them -- bounding the economy, implementing
    #   CREDIT_TO_AP_RATE, and valuing production capacity -- each found and fixed a GENUINE
    #   defect and each moved resolution NOT AT ALL. The two that moved it were about
    #   INCENTIVES: production cooldowns (slower reinforcement), and decisively, making the
    #   objective outscore trading. _combat_value scaled an HQ hit by hp_removed/max_hp, so a
    #   5-damage chip scored 0.75 against 3.00 for a Trooper kill -- units correctly broke off
    #   a reachable objective to fight. hq_siege_value 12 -> 60 (break-even 48).
    #   ⇒ s6-05's own PREDICTION ("a kill still outscores an HQ chip 4:1 -- that may be correct
    #     play or the trading pattern surviving; THE BATCH DECIDES") was right, and it was the
    #     trading pattern. The batch decided.
    # ⚠ The 3 games that go the distance are the +0 MIRROR -- a deterministic AI on a symmetric
    #   board should tiebreak. But the harness gives that cell n=1, NOT n=3: `variant` perturbs
    #   bonus-unit placement and at +0 handicap there are no bonus units, so all three are
    #   byte-identical. Fix the harness before that cell is used to conclude anything.
    # ⚠ Read 18/18 handicap conversion SCEPTICALLY: the gate asked for "more often than not"
    #   and got TOTAL correlation. Right direction, but it suggests the slice has no comeback
    #   mechanism. Worth a look during balance work.
    # ./redot --headless tools/SimulateMatches.tscn (~25 min for a full batch)
    # PASS = games resolve on PLAY (HQ destruction) not the round cap; outcomes appear
    # from BOTH seats; the material-advantaged side wins more often than not; non-zero
    # HQ damage exists at all. Baseline to beat: 0/20 resolving, 0 HQ damage, 95-100% P1.
    # ★ RUN IT EARLY -- after s6-02 and again after s6-04. If bounding the economy alone
    #   is insufficient, PIVOT-NOTE 3.2 predicts the existing siege term should surface on
    #   its own; IF IT DOES NOT, THE DIAGNOSIS IS WRONG and that is the first thing to revisit.

  # --- Should Have ---
  - id: "s6-07"
    name: "HUD — gross / upkeep / net income + population readout"
    file: "design/gdd/game-hud.md"
    priority: should-have
    status: done
    owner: "ui-programmer"
    estimate_days: 1.0
    blocker: "s6-02, s6-04"
    completed: "2026-08-24"
    # ✅ DONE (3ddcad7). The income popover was still RENDERING base + outpost + econ_tech
    # long after s6-01 deleted the Economy Outpost: its data model had been repointed, its
    # _draw() had not, so the player saw two permanently-zero terms for mechanics that no
    # longer exist and could not see upkeep or net at all. Now gross - upkeep = net, with net
    # larger and the only coloured figure (UR-8). open_preview() projects net after a
    # prospective purchase (AC-20). New PopulationWidget for AC-12, kept SEPARATE from income
    # on purpose -- upkeep is a gradient you watch approach, the cap is a hard stop.
    # ★ A LIVE DEFECT surfaced while writing that up. AC-12's other half -- "the produce
    #   affordance is visibly disabled with a stated reason" -- was not merely unshipped. The
    #   cap was enforced in the rules (BaseProduction.validate), respected by the AI, and
    #   displayed by the new widget, but THE VERB MENU NEVER CHECKED IT. A player at cap saw
    #   Produce enabled, chose a unit, and got a rejection with no forewarning. Fixed in
    #   CommandFSM.
    # ⚠ Presentation call that is the USER'S to overrule (unit-upkeep UOQ-5, reversible):
    #   net gets the visual weight rather than all three figures being equal.
    # Cross-review B-3: the spec mandated base + outpost + econ_tech, two of which no
    # longer exist. Replacement: gross - upkeep = net, which also gives unit-upkeep UR-8's
    # three-figure contract a home it never had in the HUD spec.
    # ★ `net` carries the visual weight -- a player approaching the upkeep equilibrium must
    #   SEE IT COMING. That early warning is most of what makes upkeep a decision, not a tax.
    # Also: purchase preview shows its effect on net (AC-20); population current/max always
    # visible; a capped player is told WHY they cannot produce.

  - id: "s6-08"
    name: "Victory / defeat presentation"
    file: "design/gdd/game-hud.md"
    priority: should-have
    status: done
    owner: "ui-programmer"
    estimate_days: 1.0
    blocker: "s6-06"
    completed: "2026-08-24"
    # ✅ DONE (9c65447). Both of CR-9's clauses, which needed genuinely DIFFERENT presentation
    # rather than one path with a swapped noun: an HQ kill explains itself; a round-limit
    # finish explains nothing (nothing died, the board still looks playable), so that path
    # also shows the deciding metric and both scores.
    # ★ Required RECORDING WHY the match ended: the deciding HQ is already erased from
    #   entities_by_id by the time the HUD reads terminal state, so the cause is unrecoverable
    #   after the fact. Adds GameState.WinReason + win_reason, and reason/metric/
    #   metric_by_player on GameOverEvent.
    # ★★ AC-22 HAD BEEN SILENTLY LIVE FOR A SPRINT. It was marked "deferred -- not testable in
    #   VS scope, activate when MAX_ROUNDS ships". MAX_ROUNDS shipped in s6-03, and ~1 game in
    #   7 was ending on it, untested, with no presentation. Nothing connected arming the round
    #   cap to that AC's activation condition. ⇒ LESSON, recorded in game-hud.md: a deferral
    #   whose trigger is another story's side effect WILL NOT RE-OPEN ITSELF -- name the story
    #   that satisfies it.
    # ⚠ Presentation call that is the USER'S to overrule (game-hud OQ-2, reversible): a capped
    #   game shows the deciding metric and both scores.
    # game-hud CR-9 / AC-17 / AC-22, unimplemented and carried since Sprint 5. A one-line
    # status message is the current stopgap. ★ Invisible while games never ended; immediately
    # player-facing the moment they do -- which is what this sprint is trying to make true.

  # --- Nice to Have (rides the buffer) ---
  - id: "s6-09"
    name: "/propagate-design-change sweep — reciprocity, pre-rescale arithmetic, naming"
    file: "design/gdd/gdd-cross-review-2026-08-24.md"
    priority: nice-to-have
    status: done
    owner: "producer"
    estimate_days: 0.5
    blocker: ""
    completed: "2026-08-24"
    # ✅ DONE (f959146 + 3 doc commits). W-1 reciprocity restored across 12 GDDs. W-5 naming
    # triaged BY CONTEXT, not find-replaced -- three documents (base-production, ap-economy,
    # research-tech) turned out to have the S6 rework bolted on top with the entire superseded
    # body left underneath IN THE PRESENT TENSE, and now carry dividers. W-4 handled with scale
    # notes rather than rewritten numbers. Plus the entity registry, four sprints stale and the
    # baseline /consistency-check trusts.
    # ★ Which surfaced the bigger problem: the FACTORY PRODUCES NOTHING (ground vehicles are
    #   wave 2) and grants no income, yet was offered in the build roster. Re-statting it to
    #   base-production.md's roster table (1,000 / 3 / 200, from the renamed Economy Outpost's
    #   400 / 1 / 100) made the trap three times more expensive. PULLED FROM THE ROSTER until
    #   it can build something. The AI already skipped it unprompted; this only ever cost the
    #   human.
    # ★ Also fixed the same rename's second inheritance: the AI's economy-investment throttle
    #   still counted a Factory build as an economy action, in BOTH the shipped driver AND
    #   simulate_matches.gd's hand-copied duplicate -- the one whose own comment demands it
    #   "match EXACTLY ... a looser rule here would silently simulate a different AI than the
    #   one that ships". A new parity suite pins it.
    # ⚠ FLAGGED FOR A DIRECTION CALL, NOT CHANGED: Barracks (data 900 vs table 600) and
    #   Defensive Structure (600/1 vs 500/2) drift the same way the Factory did -- but they are
    #   LIVE BALANCE VALUES on a gate that has only just started passing, and Barracks
    #   throughput was one of the two levers that fixed it. Re-statting them is a balance
    #   decision, not a cleanup.
    # Cross-review W-1 (0/11 dependency reciprocity), W-4 (pre-rescale arithmetic in prose
    # across 5 Approved docs), W-5 ("Production Outpost" naming in ~10 docs). Mechanical --
    # no conclusions change, the rescale is proportional. APOQ-SCALE-2.

  - id: "s6-10"
    name: "Factory art — re-point the retired Economy Outpost assets"
    file: "design/assets/specs/vs-entities-assets.md"
    priority: nice-to-have
    status: in-progress
    owner: "art-director"
    estimate_days: 0.5
    blocker: "s6-03"
    completed: ""
    # ◐ PARTIALLY DONE, and not by this story. s6-03's rename COPIED rather than moved: all
    # seven struct_factory_* PNGs + sidecars exist and are BYTE-IDENTICAL to the seven
    # struct_economy_outpost_* files, WHICH ARE STILL IN THE TREE. Runtime already reads the
    # factory names, so the duplicates are inert weight, not a live fork -- but they are a
    # trap: an art edit to "the factory sprite" has two plausible targets and only one of them
    # renders. ⇒ REMAINING: delete the 7 stale PNGs + 7 sidecars, and update the manifest,
    # entity inventory and vs-entities-assets.md spec text. Still zero new generations.
    # User-approved 2026-08-24: reuse struct_economy_outpost_{rush,boom,neutral}_{idle,
    # destroyed} + _idle_glow as struct_factory_*. Seven files + .import sidecars, manifest,
    # entity inventory, and the spec text. ZERO new generations.

  # --- Carried, human-gated, NOT on this sprint's critical path ---
  - id: "s5-03"
    name: "★ Iso-legibility playtest (Pillar-3 hard gate) — carried from Sprint 5"
    file: "production/playtests/iso-legibility-playtest-2026-07-29.md"
    priority: carried
    status: ready-for-dev
    owner: "user"
    estimate_days: 0.5
    blocker: ""
    completed: ""
    # ★ FOUR sprints. Blocked on the user, not on us. It is also the input to cross-review
    # B-4 (the Pillar-3 legibility budget) -- the corpus has since added unit classes
    # (incl. hovering air), damage types, 3 area telegraphs, 8 abilities, ranks, upkeep
    # readouts and crew states onto a board that already took a full sprint to make legible.
    # SIX GDDs each flag their own visual need; NOTHING OWNS THE AGGREGATE.
    # ⇒ Answer B-4 before any wave-2 renderer or art work.

  - id: "s5-04"
    name: "★ Swing-back playtest — carried, DEFERRED until after the s6-06 gate"
    file: "production/playtests/swing-back-playtest-session-2026-08-19.md"
    priority: carried
    status: ready-for-dev
    owner: "user"
    estimate_days: 1.0
    blocker: ""
    completed: ""
    # ★★ UNBLOCKED 2026-08-25 -- s6-06 PASSED. The deferral condition is met: the AI can now
    # actually lose, and matches resolve on play at a mean 25 turns, so this session finally
    # measures a real game instead of judging a build whose central failure was already known.
    # ⇒ THIS IS NOW THE HIGHEST-VALUE ITEM ON THE BOARD, and it needs the user at a display.
    # The session file production/playtests/swing-back-playtest-session-2026-08-19.md is open
    # but ENTIRELY EMPTY: every per-game row and every Analysis A-D answer is a placeholder.
    # ★ Deliberately deferred, and this is a change from three sprints of trying to run it.
    # Playing it now would judge a build whose central failure is already known and already
    # scheduled for repair -- the same mistake that rolled it over three times. After s6-06
    # passes, the AI can actually lose, and the session finally measures a real game.

  - id: "s5-07"
    name: "★ Windowed Visual/Feel sign-offs — carried, captures done"
    file: "production/qa/evidence/s5-07-windowed/README.md"
    priority: carried
    status: review
    owner: "art-director + qa-lead"
    estimate_days: 0.5
    blocker: ""
    completed: ""
    # 11 frames captured from the real rasteriser 2026-08-21. The captures exist; the human
    # sign-off does not.
